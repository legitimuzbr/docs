---
title: 'Instalação'
description: 'Esse é seu guia de instalação'
icon: 'heart'
---

### Exemplo

Para instalar o Face Index SDK em seu projeto, copie e cole o código abaixo na página que deseja utilizar o SDK.

```html HTML
<div id="legitimuz-root"></div>
<script src="https://cdn.legitimuz.com/js/sdk/faceindex.js"></script>
<script lang="text/javascript">
  const sdkInstance = LegitimuzFaceIndex({
    apiURL: "https://api.legitimuz.com",
    appURL: "https://liveness.legitimuz.com",
    token: "seu-token-de-integração",
  });

  // Montar SDK
  sdkInstance.mount();
</script>
```

### Uso

Antes de implementar o código a seguir, é importante garantir que a página
contenha os elementos HTML necessários.
O Face Index SDK requer um elemento de entrada [input](https://developer.mozilla.org/pt-BR/docs/Web/HTML/Element/Input)
com o atributo [id](https://developer.mozilla.org/pt-BR/docs/Web/HTML/Global_attributes/id) definido
como [legitimuz-hydrate-cpf](./fields).
Este elemento é usado para a entrada do CPF.

```html
<input id="legitimuz-hydrate-cpf" type="tel" max-length="11" />
```

Além disso, a página deve incluir um botão [button](https://developer.mozilla.org/pt-BR/docs/Web/HTML/Element/button)
com o atributo id definido como [legitimuz-action-faceindex](./actions).
Esse botão é essencial para iniciar o processo de verificação.

<Warning>
  Certifique-se de que esses elementos estejam presentes na página antes de
  prosseguir com a implementação do código acima.
</Warning>

<Tip>
  Se seu código possue um padrão de escrita diferente?
  fique tranquilo, você pode alterar o seletor dos elementos através dos [métodos](./actions)
  exportados pelo SDK.
</Tip>
